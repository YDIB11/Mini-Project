# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtual environments
.venv/
venv/
env/
ENV/

# IDE / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Data: never committed (HateMM is downloaded once per machine)
data/*
!data/.gitkeep

# Derived artefacts (top-level dirs only; do not match src/features/)
/cache/
/features/
/checkpoints/
/runs/
/wandb/
/logs/
/outputs/
*.pt
*.pth
*.ckpt
*.npy
*.npz

# LaTeX build intermediates
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.synctex.gz
paper/*.fls
paper/*.fdb_latexmk
paper/*.toc

# Hugging Face cache (encoder weights live here on first run)
.cache/
hf_cache/

# Env / secrets
.env
.env.local
