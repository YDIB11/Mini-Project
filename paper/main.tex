% EE-559 Mini-Project: Multimodal Hate Video Classification on HateMM.
%
% Drop-in replacement for main.tex in the EE-559 template. To compile:
%   1. Place this file alongside main.bib and the source/ folder from the
%      EE_559__Group_Mini_Project_Template.
%   2. Fill in the group number (search for GroupNumber).
%   3. pdflatex main && bibtex main && pdflatex main && pdflatex main.

\documentclass{article}

% Style files from the EE-559 template.
\input{source/preamble}
\usepackage[accepted]{source/icml2021}
\usepackage{amssymb}   % for \mathbb in the Method section
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, calc}

%%%%%%%%% GROUP NUMBER %%%%%%%%%
\icmltitlerunning{EE-559 Deep Learning: Group Mini-Project, Group 7}

\begin{document}

\twocolumn[
\icmltitle{Multimodal Fusion for Hate Video Classification on HateMM}

\begin{icmlauthorlist}
\icmlauthor{Youssef Dib}{ch} \quad
\icmlauthor{Kevin Abou Jaoude}{ch} \quad
\icmlauthor{Rami Aschkar}{ch} \\
\end{icmlauthorlist}

\icmlaffiliation{ch}{\'Ecole Polytechnique F\'ed\'erale de Lausanne}
\vskip 0.05in
\centering
%%%%%%%%% GROUP NUMBER %%%%%%%%%
Group 7
\vskip 0.3in
]

\printAffiliationsAndNotice{}

\begin{abstract}
We investigate multimodal fusion for binary hate-video classification on HateMM, a 1083-video benchmark sourced from BitChute. Our approach extracts per-modality features with three frozen pretrained encoders (CLIP for frames, wav2vec2 for audio, RoBERTa for transcripts) and trains a small transformer fusion head on the cached features. Averaged over three random seeds, the fusion model attains test AUROC of $0.876 \pm 0.012$, a robust 5-point improvement over the strongest unimodal baseline (text-only, AUROC $0.827 \pm 0.016$). On threshold-dependent metrics (accuracy, macro-F1) the fusion model matches the text-only baseline on average with markedly higher per-seed variance, indicating that fusion improves probability ranking more reliably than it improves a fixed-threshold prediction.

\textbf{Keywords:} multimodal learning, hate speech detection, video classification, frozen encoders, transformer fusion.

\end{abstract}

%%%%%%%%% INTRODUCTION %%%%%%%%%
\section{Introduction}
\label{sec:intro}

Hate-speech moderation on user-generated video platforms is challenging precisely because hateful intent often emerges from the joint configuration of multiple modalities: what is shown, what is said, and how it is delivered. A text classifier can miss a slur delivered tonally; a visual classifier can miss harmful framing of an otherwise neutral image. Unimodal systems thus risk both false negatives on cross-modal hate and false positives on visually charged but linguistically innocuous content.

Most public benchmarks for hate detection have focused on text or memes \cite{kiela2020hateful}. The recent HateMM dataset \cite{das2023hatemm} closes part of this gap by providing 1083 BitChute videos with binary hate labels and per-video target-community annotations. We use HateMM to study a focused question: how much does fusing video, audio, and text improve over the best unimodal classifier, and is the gain consistent across target communities?

We adopt a frozen-encoder pipeline. Pretrained vision, audio, and text models produce per-video feature sequences that are cached to disk, and a small transformer fusion head is trained on top. Freezing the encoders keeps training cost low and, crucially, makes the modality ablation interpretable: each unimodal baseline uses the same architecture as the fusion model, restricted to a single modality, so the comparison isolates the contribution of additional modalities rather than additional parameters.

%%%%%%%%% RELATED WORK %%%%%%%%%
\section{Related Work}
\label{sec:related_work}

Multimodal hate detection has been studied on image-text memes \cite{kiela2020hateful} and a range of audio-text pairs. \cite{das2023hatemm} introduce HateMM and report several baselines, including single-modality CLIP plus MLP and an XGBoost late-fusion classifier over per-modality embeddings. Our approach differs in two respects: (i) we freeze all unimodal encoders to factor out training-time confounders from the ablation, and (ii) we adopt an early-fusion transformer at the token level rather than a late-fusion ensemble of per-modality classifiers.

%%%%%%%%% METHOD %%%%%%%%%
\section{Method}
\label{sec:method}

\begin{figure*}[t]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
    >=Stealth,
    node distance=8mm and 6mm,
    enc/.style={draw, rounded corners=2pt, minimum height=8mm, minimum width=20mm,
                fill=gray!12, draw=gray!50, align=center, font=\scriptsize},
    train/.style={draw, rounded corners=2pt, minimum height=8mm, minimum width=20mm,
                  fill=yellow!22, draw=yellow!50!black, align=center, font=\scriptsize},
    io/.style={align=center, font=\scriptsize},
    out/.style={align=center, font=\scriptsize\bfseries, color=yellow!40!black},
    arr/.style={->, semithick, gray!70},
    dim/.style={font=\tiny, midway, above=-1pt, color=gray!70!black},
]
% Inputs
\node[io] (vin) {Video\\frames};
\node[io, below=10mm of vin] (ain) {Audio\\track};
\node[io, below=10mm of ain] (tin) {Transcript};

% Frozen encoders
\node[enc, right=of vin] (clip) {CLIP ViT-B/32\\(frozen)};
\node[enc, right=of ain] (w2v)  {wav2vec2-base\\(frozen)};
\node[enc, right=of tin] (rob)  {RoBERTa-base\\(frozen)};

% Trainable per-modality projections
\node[train, right=of clip] (vp) {Linear\\$\to d_m$};
\node[train, right=of w2v]  (ap) {Linear\\$\to d_m$};
\node[train, right=of rob]  (tp) {Linear\\$\to d_m$};

% Fusion column
\node[train, right=12mm of ap, minimum width=22mm] (fuse) {[CLS] $\oplus$\\concat};
\node[train, right=of fuse, minimum width=28mm] (tx) {Transformer\\2 layers, 4 heads\\$d_m{=}256$};
\node[train, right=of tx, minimum width=20mm] (head) {[CLS] readout\\$\to$ Linear};

% Output
\node[out, right=of head] (out) {hate /\\non-hate};

% Inputs -> encoders
\draw[arr] (vin) -- (clip);
\draw[arr] (ain) -- (w2v);
\draw[arr] (tin) -- (rob);

% Encoders -> projections, with sequence-length labels
\draw[arr] (clip) -- node[dim] {$32{\times}512$} (vp);
\draw[arr] (w2v)  -- node[dim] {$64{\times}768$} (ap);
\draw[arr] (rob)  -- node[dim] {$128{\times}768$} (tp);

% Projections -> fusion (L-shaped routes for upper/lower modalities)
\draw[arr] (vp.east) -- ++(3mm,0) |- (fuse.west);
\draw[arr] (ap.east) -- (fuse.west);
\draw[arr] (tp.east) -- ++(3mm,0) |- (fuse.west);

% Fusion -> transformer -> readout -> output
\draw[arr] (fuse) -- (tx);
\draw[arr] (tx) -- (head);
\draw[arr] (head) -- (out);
\end{tikzpicture}%
}
\caption{Pipeline architecture. Frozen pretrained encoders (grey) produce per-modality feature sequences; trainable layers (gold) project them into a common $d_m{=}256$ embedding space, prepend a learnable [CLS] token, fuse them with a small transformer encoder, and emit a binary prediction from the [CLS] readout.}
\label{fig:arch}
\end{figure*}

\textbf{Preprocessing.} For each video we extract three modality streams. We sample 32 RGB frames at 1\,fps with center cropping and resize to $224 \times 224$. The audio track is decoded to a 16\,kHz mono PCM stream with \texttt{ffmpeg}; videos lacking an audio stream are replaced with a 0.5\,s silent placeholder so downstream code receives a uniform tensor shape. The English transcript is obtained with faster-whisper \cite{radford2022whisper} running the \texttt{small} model on the extracted audio.

\textbf{Frozen feature extraction.} Three pretrained encoders, all kept frozen, produce per-modality feature sequences. Video frames are passed through the CLIP ViT-B/32 vision tower \cite{radford2021clip}, yielding a sequence of 32 frame embeddings in $\mathbb{R}^{512}$. Audio is encoded with wav2vec2-base \cite{baevski2020wav2vec2}; the resulting hidden states are mean-pooled into 64 fixed-length tokens in $\mathbb{R}^{768}$ to bound memory across variable-length inputs. Transcripts are passed through RoBERTa-base \cite{liu2019roberta} with truncation at 128 tokens, producing token-level embeddings in $\mathbb{R}^{768}$ with the standard attention mask. All per-video features are cached to disk so subsequent experiments operate on cheap tensor reads.

\textbf{Fusion transformer.} As shown in Fig.~\ref{fig:arch}, per-modality features are linearly projected to a common 256-dimensional embedding space and tagged with a learned modality identifier. A trainable \texttt{[CLS]} token is prepended and the concatenated sequence is processed by a pre-norm transformer encoder \cite{vaswani2017attention} with two layers and four attention heads, GELU activations, and dropout 0.1. The \texttt{[CLS]} output is fed to a linear binary classifier. The same module supports arbitrary modality subsets by omitting the corresponding embedding sequence, which we use to obtain unimodal baselines that share the exact same architecture as the full fusion model. This makes the ablation a clean comparison of \emph{modality information}, not of architecture or capacity.

%%%%%%%%% VALIDATION %%%%%%%%%
\section{Validation}
\label{sec:validation}

\textbf{Dataset and splits.} HateMM \cite{das2023hatemm} contains 1083 BitChute videos (431 hate, 652 non-hate) released under CC BY 4.0, each carrying a binary hate label and a target-community annotation. After preprocessing, 1081 of the 1083 videos produced usable features. We split stratified by binary label into 80/10/10 train, validation, and test partitions (863/109/109 videos), with a fixed random seed.

\textbf{Training.} All models are optimised with AdamW \cite{loshchilov2019adamw} at a learning rate of $10^{-4}$, weight decay $10^{-2}$, and batch size 16, using binary cross-entropy loss. We train for up to 20 epochs with early stopping on validation macro-F1 (patience 5). Each modality configuration is trained under three random seeds $\{42, 1337, 2025\}$, with the data split held fixed across seeds for fair comparison; results below are reported as mean $\pm$ standard deviation across seeds. All runs use a single NVIDIA V100 GPU.

\textbf{Modality ablation.} Tab.~\ref{tab:ablation} and Fig.~\ref{fig:ablation_bars} report test-set metrics for the three unimodal models and the trimodal fusion as mean $\pm$ standard deviation over three seeds. The fusion model achieves the highest AUROC by a robust margin ($0.876 \pm 0.012$), an absolute gain of $4.9$ AUROC points over the strongest unimodal baseline (text, $0.827 \pm 0.016$); the gap exceeds twice the combined standard deviation. On threshold-dependent metrics (accuracy, macro-F1) the fusion model matches the text-only baseline on average but with markedly higher per-seed variance (accuracy standard deviation $0.052$ for fusion versus $0.021$ for text), indicating that the multimodal model provides better calibrated probability rankings rather than a uniform threshold-level gain. Audio is the weakest unimodal baseline; text is the strongest, consistent with the verbal nature of much hate content in this dataset.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/ablation_bars}
\caption{Multi-seed test-set performance on HateMM, sorted by AUROC. Error bars show $\pm 1$ standard deviation across three seeds. AUROC (gold) shows a clear and robust separation between the trimodal fusion and each unimodal baseline. On accuracy (blue) the fusion model overlaps with the text-only baseline once seed variance is taken into account.}
\label{fig:ablation_bars}
\end{figure}

\begin{table}[t]
\vskip 0.15in
\begin{center}
\begin{small}
\begin{sc}
\begin{tabular}{lccccc}
\toprule
Modalities & Acc & F1 & Prec & Rec & AUROC \\
\midrule
Video & $.72{\pm}.03$ & $.71{\pm}.02$ & $.64{\pm}.06$ & $.68{\pm}.06$ & $.81{\pm}.01$ \\
Audio & $.67{\pm}.02$ & $.67{\pm}.03$ & $.56{\pm}.02$ & $.71{\pm}.05$ & $.77{\pm}.03$ \\
Text  & $.79{\pm}.02$ & $.78{\pm}.02$ & $.75{\pm}.06$ & $.73{\pm}.08$ & $.83{\pm}.02$ \\
\midrule
V+A+T & $.79{\pm}.05$ & $.78{\pm}.05$ & $.74{\pm}.08$ & $.74{\pm}.08$ & $\mathbf{.88{\pm}.01}$ \\
\bottomrule
\end{tabular}
\caption{Test-set metrics on HateMM ($n=109$), mean $\pm$ standard deviation over three seeds. The fusion model wins robustly on AUROC; on threshold-dependent metrics it matches the text-only baseline on average with higher per-seed variance.}
\label{tab:ablation}
\end{sc}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

\textbf{Per-target-community audit.} Tab.~\ref{tab:community} reports per-modality test accuracy on the three target-community groups for which the test split contains at least 15 examples, as mean $\pm$ standard deviation over three seeds. The per-community picture is consistent with the overall one: fusion's advantage shrinks once seed variance is taken into account. On \emph{Blacks} ($n=46$) fusion marginally outperforms text by $0.022$ accuracy points; on \emph{Others} ($n=39$) text edges out fusion by $0.017$; on \emph{Jews} ($n=15$) text outperforms fusion by $0.022$ and the fusion model exhibits a markedly larger per-seed variance ($0.13$ versus $0.04$ for text), indicating that the higher-capacity fusion is unstable when conditioned on a small subgroup. All three gaps lie well within the combined standard deviation. We also note that on \emph{Others} the macro-F1 collapses to approximately $0.5$ for every model despite accuracy of $\sim 0.85$, a label-imbalance effect: the group is dominated by non-hate examples and every model defaults to the majority class on the few hate cases. The remaining target-community labels in HateMM appear in too few test examples to support per-group conclusions; we report only the three groups with sufficient test support.

\begin{table}[t]
\vskip 0.15in
\begin{center}
\begin{small}
\begin{sc}
\begin{tabular}{lccccc}
\toprule
Community & $n$ & V & A & T & V+A+T \\
\midrule
Blacks & 46 & $.70{\pm}.04$ & $.59{\pm}.06$ & $.73{\pm}.03$ & $\mathbf{.75{\pm}.06}$ \\
Others & 39 & $.74{\pm}.09$ & $.68{\pm}.11$ & $\mathbf{.86{\pm}.05}$ & $.84{\pm}.02$ \\
Jews   & 15 & $.67{\pm}.07$ & $.71{\pm}.04$ & $\mathbf{.76{\pm}.04}$ & $.73{\pm}.13$ \\
\bottomrule
\end{tabular}
\caption{Per-target-community test accuracy (groups with $n \geq 15$), mean $\pm$ standard deviation over three seeds. Differences between fusion and the strongest unimodal baseline are within combined seed variance for all three groups.}
\label{tab:community}
\end{sc}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

\textbf{Discussion and limitations.} Our headline finding is nuanced: a small, frozen-encoder fusion model robustly improves AUROC over each unimodal baseline, but on threshold-dependent metrics it matches the strongest unimodal model (text) on average with higher per-seed variance. This pattern, a robust ranking gain accompanied by a noisy threshold-level gain, is consistent with a higher-capacity model that learns better discrimination but is more sensitive to optimisation randomness in the loss-to-prediction map. A single-seed report would have overstated the threshold-level improvement; multi-seed analysis surfaces this nuance. The per-community audit reinforces the same conclusion: at the subgroup level fusion does not consistently outperform text, and on the smallest reported group (\emph{Jews}, $n=15$) the fusion model's seed variance exceeds its mean advantage by an order of magnitude.

Several limitations remain. First, the test set ($n=109$) is small, so per-community estimates carry appreciable variance; bootstrap confidence intervals or substantially larger test support would tighten the interpretation further. Second, our community-label normalisation is heuristic: multi-community labels are canonicalised to a sorted comma-joined form, which conflates intersectional rows with singleton ones. Third, the audio encoder pools the wav2vec2 hidden states to a fixed length of 64 tokens, which over-averages long videos and pads short ones; an adaptive pooling scheme may help the audio-only baseline.

%%%%%%%%% CONCLUSION %%%%%%%%%
\section{Conclusion}
\label{sec:conclusion}

We presented a small, frozen-encoder multimodal classifier for hate video detection on HateMM. Averaged over three seeds, the model attains test AUROC of $0.876 \pm 0.012$, a robust 5-point improvement over each unimodal baseline. On threshold-dependent metrics the gain washes out into seed variance, a finding a single-seed report would have missed. The pipeline is fully reproducible from the released dataset and our public code repository.

% \newpage

%%%%%%%%% REFERENCES %%%%%%%%%
\bibliographystyle{ieeetr}
\bibliography{main}

\end{document}
